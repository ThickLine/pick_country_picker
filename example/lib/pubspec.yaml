name: pick_country_picker_example
description: Example app for the pick_country_picker package
publish_to: "none" # Prevents accidental publication

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  pick_country_picker:
    path: ../../../pick_country_picker

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
